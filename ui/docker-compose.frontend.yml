services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    image: ui
    container_name: ui
    environment:
      PORT: "5173"
      API_TARGET: "http://api:5001"
    ports:
      - "5173:5173"
    restart: unless-stopped
